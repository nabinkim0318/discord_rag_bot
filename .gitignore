# ========================
# Global (cross-project)
# ========================
# OS / editor
.DS_Store
Thumbs.db
ehthumbs.db
Desktop.ini
.vscode/
.idea/
*.swp
*.bak
*.tmp

# Python bytecode / caches
**/__pycache__/
**/*.py[cod]
**/*.pyo
**/*.pyd
**/.pytest_cache/
**/.mypy_cache/
.ipynb_checkpoints/

# Logs & build artifacts (all projects)
**/logs/
**/*.log
**/dist/
**/build/
coverage/
.coverage
htmlcov/

# Env files (keep only examples in VCS)
.env
.env.*
**/.env
**/.env.*

# SQLite & generic DB files
*.sqlite3
*.db


# ========================
# Python (backend, rag_agent)
# ========================
# Virtualenvs
venv/
.venv/

# Project-local ignores
backend/.env
rag_agent/.env

# If backend uses sqlite locally
backend/db.sqlite3

# (Optional) rag_agent data & artifacts kept out of git
rag_agent/data/
!rag_agent/data/README.md


# ========================
# Node / Next.js (frontend, discord bot)
# ========================
# Node modules & Next outputs
**/node_modules/
**/.next/
**/out/

# Frontend env variants (Next)
frontend/.env.local
frontend/.env.development.local
frontend/.env.test.local
frontend/.env.production.local

# Package managers logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# ========================
# Containers / DevOps
# ========================
# DO NOT ignore Dockerfiles by default (we want them tracked)
# Docker Compose override (local-only)
**/docker-compose.override.yml

# (optional) local compose profiles
**/.compose/

# .dockerignore should be tracked; do not ignore it


# ========================
# Monitoring / Tooling
# ========================
# Keep Grafana/Prometheus configs tracked; add project-specific ignores below if needed
# (nothing here by default)


# ========================
# Frontend tooling
# ========================
# Jest / SWC / Babel caches (safe to ignore if present)
**/.cache/
